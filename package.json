{
  "name": "checkin-system",
  "version": "1.0.0",
  "description": "Offline LAN check-in board, one server for any number of screens and buildings, no external dependencies.",
  "private": true,
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "import-people": "node server/import-people.js",
    "dev": "bash scripts/dev.sh"
  },
  "engines": {
    "node": ">=14"
  }
}
